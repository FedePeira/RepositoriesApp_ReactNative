import { StyleSheet } from 'react-native';

const MenuFilterStyles = StyleSheet.create({
  menu: {
    padding: 10, 
    width: '60%', 
    alignSelf: 'center', 
    marginTop: 10,
  },
});

export default MenuFilterStyles;